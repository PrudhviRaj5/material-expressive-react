name: Deploy Storybook to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install docs dependencies
        run: cd docs && npm ci

      - name: Compute Storybook base path (user pages vs project pages)
        id: base
        shell: bash
        run: |
          REPO_NAME='${{ github.event.repository.name }}'
          OWNER='${{ github.repository_owner }}'

          # User/org pages are served from the root at https://<owner>.github.io/
          # Project pages are served under https://<owner>.github.io/<repo>/
          if [[ "$REPO_NAME" == "$OWNER.github.io" ]]; then
            echo "storybook_base=/storybook/" >> "$GITHUB_OUTPUT"
            echo "docs_base=/docs/" >> "$GITHUB_OUTPUT"
            echo "storybook_url=https://$OWNER.github.io/storybook/" >> "$GITHUB_OUTPUT"
          else
            echo "storybook_base=/$REPO_NAME/storybook/" >> "$GITHUB_OUTPUT"
            echo "docs_base=/$REPO_NAME/docs/" >> "$GITHUB_OUTPUT"
            echo "storybook_url=https://$OWNER.github.io/$REPO_NAME/storybook/" >> "$GITHUB_OUTPUT"
          fi

      - name: Build combined Pages artifact (landing + storybook + docs)
        env:
          STORYBOOK_BASE_PATH: ${{ steps.base.outputs.storybook_base }}
          DOCUSAURUS_BASE_URL: ${{ steps.base.outputs.docs_base }}
          STORYBOOK_URL: ${{ steps.base.outputs.storybook_url }}
        run: npm run pages:build

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: pages-dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
